@page "/settings"

<div class="app-page">
    <header class="app-header">
        <button class="icon-btn" @onclick='() => Nav.NavigateTo("/")'>
            <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                <line x1="19" y1="12" x2="5" y2="12"></line>
                <polyline points="12 19 5 12 12 5"></polyline>
            </svg>
        </button>
        <h1 class="header-title">Impostazioni</h1>
        <div style="width: 40px;"></div> <!-- Spacer to center title -->
    </header>

    <div class="content">
        <div class="card-vibe">
            <div class="form-group-vibe mb-4">
                <label class="section-header-vibe">NOME AUTORE</label>
                <div class="input-wrapper">
                    <input type="text" class="form-control-vibe" 
                           @bind="authorName" @bind:event="oninput" />
                    <span class="input-icon">
                        <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                            <path d="M11 4H4a2 2 0 0 0-2 2v14a2 2 0 0 0 2 2h14a2 2 0 0 0 2-2v-7"></path>
                            <path d="M18.5 2.5a2.121 2.121 0 0 1 3 3L12 15l-4 1 1-4 9.5-9.5z"></path>
                        </svg>
                    </span>
                </div>
                <p class="help-text">Questo nome apparirà nei tuoi documenti ufficiali.</p>
            </div>

            <div class="form-group-vibe">
                <label class="section-header-vibe">DEVICE ID</label>
                <div class="input-wrapper">
                    <input type="text" class="form-control-vibe readonly-input" 
                           value="@DisplayDeviceId" readonly />
                    <span class="input-icon">
                        <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                            <rect x="3" y="11" width="18" height="11" rx="2" ry="2"></rect>
                            <path d="M7 11V7a5 5 0 0 1 10 0v4"></path>
                        </svg>
                    </span>
                </div>
                <p class="help-text">L'identificativo univoco del dispositivo non può essere modificato.</p>
            </div>
        </div>
   
        <button class="btn-vibe btn-primary-vibe" disabled="@(string.IsNullOrWhiteSpace(authorName))" @onclick="SaveSettings">Salva</button>
    </div>
</div>

<style>
    .input-wrapper {
        position: relative;
        display: flex;
        align-items: center;
    }

    .input-icon {
        position: absolute;
        right: 1.25rem;
        color: #20563C;
        display: flex;
    }

    .readonly-input + .input-icon {
        color: #ADB5BD;
    }

    .help-text {
        font-size: 0.85rem;
        color: #6C757D;
        margin-top: 0.75rem;
        font-style: italic;
    }

    .mb-4 { margin-bottom: 1.5rem; }
</style>
