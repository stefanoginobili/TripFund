**Task 5: Trip & Member Dashboards**

1. Implement **Flow 4 (Trip Dashboard)** from `docs/UX_FLOW.md`. Implement the exact mathematical logic for the 'Quota Residua' using the `split` dictionary.
2. Implement **Flow 8 (Member Dashboard)**. Ensure the transactions list filters and calculates amounts specifically for the viewed user.
3. Provide narrow UI integration tests (e.g., using bUnit) to verify the math on the dashboards updates correctly based on the selected currency.