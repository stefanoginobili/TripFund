**Task 3: Global Settings & Landing Page**

1. Implement **Flow 10 (Global Settings / Onboarding)** from `docs/UX_FLOW.md`. Ensure the app blocks navigation until `app_settings.json` is created.
2. Implement **Flow 1 (Landing Page)** reading from `known_trips.json`.
3. Provide narrow integration tests for the first launch blocker and the rendering of the Landing Page.